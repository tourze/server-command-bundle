services:
  # default configuration for services in *this* file
  _defaults:
    autowire: true      # Automatically injects dependencies in your services.
    autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

  ServerCommandBundle\Command\:
    resource: '../../Command/'
  ServerCommandBundle\Controller\:
    resource: '../../Controller/'
  ServerCommandBundle\DataFixtures\:
    resource: '../../DataFixtures/'
  ServerCommandBundle\MessageHandler\:
    resource: '../../MessageHandler/'
  ServerCommandBundle\Repository\:
    resource: '../../Repository/'
  ServerCommandBundle\Service\:
    resource: '../../Service/'
